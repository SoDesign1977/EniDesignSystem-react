import React from "react";

import EniDatePicker from "./EniDatePicker";

export default {
  component: EniDatePicker,
  title: 'Components/DatePicker/EniDatePicker',
};

const Template = (args) => <EniDatePicker {...args} />;

export const Default = Template.bind({});
Default.args = {

};
